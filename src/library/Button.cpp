//#include "Button.h"

namespace tv
{
    //Button::Button(Vector2 position, std::string txt, float bSize, Color color)
    //  :  color(color)
    //{
    //  Create(txt, bSize);
    //}

    //void Button::Draw()
    //{
    //  DrawRectangleV(position, size, color);
    //  text.Draw();
    //}

    //void Button::Create(std::string txt, float bSize)
    //{
    //  auto textSize = MeasureTextEx(GetFontDefault(), txt.c_str(), bSize, constants::TEXT_SPACING);
    //  text = Text(txt, position.x - (textSize.x/2), position.y - (textSize.y/2), bSize);
    //  this->size = Vector2(textSize.x + constants::TEXT_BUTTON_SPACING * 2, textSize.y + constants::TEXT_BUTTON_SPACING);
    //  this->position = Vector2(position.x - constants::TEXT_BUTTON_SPACING - textSize.x/2, 
    //      position.y - constants::TEXT_BUTTON_SPACING/2 - textSize.y/2);
    //}

} // namespace tv
